#include "main.h"

/**
 * mul - Multiplies two intergers
 * @x: The first number to multiply
 * @y: The second number to multiply be the first number
 * Return: The result of multiplication between x paraam and y paraam
 */

int mul(int x, int y)

{
	return (x * y);
}
